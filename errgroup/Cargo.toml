[package]
name = "errgroup"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Async task group with error propagation and optional concurrency limiting"

[lib]
path = "errgroup.rs"

[dependencies]
tokio = { workspace = true }
