[package]
name = "crypto"
version = "0.1.0"
edition = "2024"
authors = ["Sylvain Kerkour"]

[lib]
path = "./src/crypto.rs"

[features]
default = ["std", "zeroize"]

alloc = []
std = ["alloc"]
zeroize = ["dep:zeroize"]

[dependencies]
p256 = { version = "0.14", default-features = false, features = ["arithmetic", "ecdh", "ecdsa", "sha256"] }
zeroize = { workspace = true, optional = true }

[dev-dependencies]
criterion = { workspace = true }
hex = { path = "../hex" }

[[bench]]
name = "hash"
harness = false

[[bench]]
name = "mac"
harness = false
